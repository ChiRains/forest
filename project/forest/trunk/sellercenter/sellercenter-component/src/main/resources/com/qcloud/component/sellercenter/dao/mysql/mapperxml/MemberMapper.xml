<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.qcloud.component.sellercenter.dao.mysql.mapper.MemberMapper">          
    <select id="list4query" parameterType="java.util.HashMap" resultType="Member">  
        select * from `sellercenter_member` where 1 = 1 
        <if test="name != null">
        	and `name` like concat('%',#{name},'%') 
        </if> 
        <if test="keyword != null">
        	and (`name` like concat('%',#{keyword},'%') or `mobile` like  concat('%',#{keyword},'%') )
        </if>          
         limit #{start},#{count}
    </select>  
    
     <select id="count4query" parameterType="java.util.HashMap" resultType="Integer">  
        select count(*) from `sellercenter_member` where 1 = 1 
        <if test="name != null">
        	and `name` like concat('%',#{name},'%') 
        </if> 
        <if test="keyword != null">
        	and (`name` like concat('%',#{keyword},'%') or `mobile` like  concat('%',#{keyword},'%') )
        </if>   
    </select> 
    
    <select id="getByAccount" parameterType="String" resultType="Member">
		select * from `sellercenter_member` where `account`=#{account}
	</select>
</mapper>